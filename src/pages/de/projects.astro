---
import BaseLayout from "@/layouts/BaseLayout.astro";
import { projects, sortProjects } from "@/data/projects.ts";
import ProjectList from "@/components/ProjectList.astro";
import { t, type Locale } from "@/i18n/ui";

const locale = (Astro.currentLocale ?? "en") as Locale;
---

<BaseLayout title={t(locale, "nav.projects")}>
  <section class="container">
    <h1>{t(locale, "nav.projects")}</h1>
    <p>{t(locale, "page.projects.subtitle")}</p>

    <ProjectList projects={sortProjects(projects)} linkable />
  </section>
</BaseLayout>
